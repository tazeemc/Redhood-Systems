# RedHood Systems - Python Dependencies
# Install with: pip install -r requirements.txt --break-system-packages

# Core dependencies
anthropic>=0.18.0
feedparser>=6.0.10
python-dotenv>=1.0.0
pandas>=2.0.0

# Optional: For Twitter scraping
tweepy>=4.14.0

# Note: Telegram scraping needs no extra dependency — public channels are
# read via the t.me/s/ web preview using the standard library.

# Optional: For data analysis
numpy>=1.24.0
matplotlib>=3.7.0

# Optional: For trade-ledger P&L (redhood_pnl.py)
# Required only if you run scoring with --with-pnl. Ticker grading and
# the rest of the pipeline work without it.
yfinance>=0.2.40

# Optional: For web interface
fastapi>=0.109.0
uvicorn>=0.27.0

# Dev / CI: tests and static analysis (see .github/workflows/ci.yml)
pytest>=8.0.0
ruff>=0.4.0
